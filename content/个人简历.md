# 黄德为｜全栈开发 · AI Agent

- 年龄：28｜工作年限：7 年
- 手机：18575255559｜邮箱：unsafeairbag@163.com
- GitHub：github.com/Wiiiiill
- 定位：全栈开发（**Go / Java 后端为主**｜Vue/TypeScript 前端｜Electron 桌面端｜**AI Agent / MCP 编排**｜**站点接入与逆向**）｜可负责从需求到交付闭环

---

## 一句话优势
交付型全栈 + AI Agent 落地：**Go/Java 后端 + MCP / Agent 编排 + Electron 桌面端 + 浏览器扩展**，能独立打通「AI 能力 → 可交付产品」的完整链路，具备带队推进与稳定性意识。

## 个人优势
- **交付闭环**：可独立完成需求沟通、方案设计、编码测试、上线与文档沉淀。
- **后端主栈（Go/Java）**：HTTP/gRPC、MySQL/PostgreSQL/Redis、鉴权与多角色边界、配置治理/计费统计、性能优化与可观测性。
- **AI Agent / MCP**：MCP Server 设计与落地（工具面映射、多租户鉴权）、LLM 接入与流式交互（SSE 思考链）、Agent 指令层调优，以及生产化收敛（错误口径、幂等重跑、可观测）。
- **客户端/多端**：Electron（IPC/Preload/自动更新/多环境打包/多站点分区隔离）+ Vue3/TS；同时覆盖 Web 与小程序。
- **站点接入与逆向**：面对无开放 API 的系统，从压缩混淆的前端产物中还原接口与业务规则，再以页面注入 + 运行时接管取得完整能力，最终抽象为 Agent 可调用的工具面；覆盖 Chrome Extension MV3（Service Worker + SidePanel + Content Scripts）与数据采集。
- **实时/任务/稳定性**：SSE/WebSocket、定时任务与 API 进程分离、缓存与队列化限流（DelayQueue）、代码评审与线上问题定位。
- **工具建设**：主导/独立落地并推广多款 Go 提效工具（路由扫描、CRUD 生成、禅道 MCP 等）。

## 技术栈（关键词）
- 前端：Vue2/3、TypeScript、Vite、Element Plus、Pinia、React、Next.js
- 桌面端：Electron（IPC / Preload / 自动更新 / 多环境打包 / 多站点分区隔离）
- 后端：**Go（HTTP/gRPC/CLI）**、**Java（Spring 系）**、Python（服务与脚本）
- AI / Agent：**MCP（Model Context Protocol）**、LLM 接入与 Prompt 工程、SSE 流式、Agent 工作流编排
- 浏览器 / 站点接入：**Chrome Extension MV3**、前端产物分析与接口还原、页面注入与运行时接管、网络请求接管与数据采集
- 存储/中间件：MySQL、PostgreSQL、MongoDB、Redis、SQLite、Nginx、gRPC、NAS 对接
- 工程化：Docker / Docker Compose、接口契约与联调、ESLint/Prettier/Commitlint

---

## 项目经历（精选）

### 海外社媒 AI 选品系统｜接单项目（主导｜2026.04 - 至今）
- 技术栈：Chrome Extension MV3 + Electron + Go + PostgreSQL + MCP + LLM
- **数据采集**：以浏览器扩展 + 页面注入复用目标站点登录态，在运行时接管其前端数据链路，实现「关键词 / 标签 / 账号 → 帖子 + 评论 + 互动指标」的稳定采集与分页；采集节奏按目标到期时间平滑铺开，显著降低账号风控命中率
- **AI 分析**：以 MCP Server 将选品知识库封装为工具面供 LLM 调用，前端接入 SSE 流式思考链与结构化结果卡片；支持模型配置级联、思考模式切换等交互能力
- **数据模型**：一源一表的采集落库设计（帖子 / 评论 / 互动快照 / 趋势分），采集批次可追溯、幂等重跑
- **工程化**：采集端与 Go 后端分离部署（Docker），含登录态门禁、请求节奏诊断脚本与数据质量校验

### Electron 多站点编排客户端｜接单项目（主导｜2026.04 - 至今）
- 技术栈：Electron + Vue3 + TypeScript + Chrome Extension MV3 + Go（MCP Server）
- **编排引擎**：将「站点打开 / 脚本注入 / 路由分发 / MCP 工具注册」四段解耦，业务与编排核心分离，新增站点只需接入一份注入适配脚本
- **多站点隔离**：登录态按「站点 × 账号」分区持久化，站点间互不串号；内置代理自动挂载与兼容处理，覆盖运行时对代理鉴权的支持限制
- **客户端能力**：皮肤远程加载与本地覆盖、分区快照导出/迁移、日志全量打包导出、登录页服务探针与网段扫描
- **交付体系**：多目标打包 + JS 混淆流水线，支持分发授权绑定与版本溯源；配套素材盲水印服务（Python，Docker 化交付）

### ERP 飞书智能助手 · 无 API 系统接入（注入 + MCP 网关）｜接单项目（主导｜2026.06 - 2026.07）
- 目标系统是一套**无开放 API、无 SDK 的生产管理 ERP**（Vue2 + Webpack 打包混淆）：前端 331 个路由、后端 1600+ 个接口，全程无任何官方对接入口
- **逆向**：分析目标系统前端产物还原完整接口清单与业务规则，输出接口 / 路由 / 技术栈 / 业务规则文档，作为后续所有能力的索引底座
- **注入接管**：通过页面注入在运行时接管前端框架的业务调用能力，直接复用页面登录态与请求上下文，无需官方对接入口即可获得完整业务能力
- **能力抽象**：把底层调用封装为领域对象（样板 / 订单 / 数据字典等），并以声明式工具描述对外暴露（含参数 schema 与可见度控制），由客户端自动注册为 MCP 工具
- **流程兜底**：对没有对应接口可用的操作，走页面级流程驱动补齐覆盖，保证能力面无死角
- **MCP 网关**：自研 Go 转发服务，Agent 侧对接标准 MCP 协议、客户端侧走长连接，内置多租户路由、任务等待/回传与会话生命周期管理
- 沉淀通用前端注入 SDK 与 MCP 网关骨架，**由前序项目直接迁移复用**（剥离原业务逻辑、保留通用骨架即可接入新站点），成为后续所有站点接入的可复用基座

### ERP 飞书智能助手 · 有 API 系统接入（OpenAPI + 业务校验）｜接单项目（主导｜2026.07 - 2026.09）
- 技术栈：Node.js / Python + 飞书开放平台 + 企业内部 ERP OpenAPI
- 以 Agent + Skill 形式落地「自然语言下单」：解析业务语义 → 按 SPU/SKC 核对主数据（款号 / 样板 / 客户）→ 调 OpenAPI 落单；主数据不一致、版单为空、输入自相矛盾一律拦截不落单
- 明确退出码语义与失败口径（「脚本跑通但存在业务失败」仍返回 0），避免下游把业务失败误判为命令失败并二次误报
- 沉淀批量主数据修正、对账导出、合同/对账模板生成等脚本工具，含审批状态锁处理与幂等重跑
- 持续调优 Agent 指令层（引导段 / 系统提示 / Skill 约束），收敛回复格式与误判路径

### AI 绘画平台 · 画布自动化与素材链路｜接单项目（主导｜2026.07 - 2026.09）
- 技术栈：Chrome Extension MV3 + 页面注入 + React / ReactFlow 运行时
- 面向 AI 绘画平台，把「建组、节点增删、位置调整、素材转存上传、画布结构备份还原」等高频人工操作做成可批量执行的能力
- **难点**：目标站点的画布由前端状态管理与多端同步驱动，常规 DOM 操作无法可靠写入；最终通过运行时接管取得权威写通道，打通建组 / 删除 / 移动 / 批量插入等此前无法自动化的操作
- **素材链路**：复用站点自身的上传与转存能力，打通「外部素材 → 平台内可用素材」的自动化通道
- **备份还原**：提供画布结构快照与个人空间结构副本，支持单画布级还原，解决批量作业中的误操作回滚
- **适配韧性**：站点构建更新会使既有接入点失效，改为运行时自动发现上传与存储链路，摆脱对固定特征名的依赖，具备自愈能力

### AI 视觉创作平台 · 素材与工作流自动化｜接单项目（主导｜2026.04 - 2026.06）
- 技术栈：Go + MongoDB + Chrome Extension MV3 + Vue3 + TypeScript + MCP
- 为该平台搭建「素材同步 → 自动上传 → 人工打标 → 自动建工作流」的完整自动化闭环
- **素材接入**：对接 NAS，按目录规约自动扫描建任务并增量同步，规避重复录入
- **标签与上传**：素材管理前端（任务列表 / 打标界面 / 快捷键操作）+ 上传服务，打标结果自动回写进度统计
- **工作流引擎**：以配置驱动工作流定义（节点构成与连线规则），Agent 经 MCP 下发任务，客户端「串行建画布 + 并行建任务」，由页面注入在画布中自动落节点与连线
- **通信链路**：Go MCP Server 作为 Agent ↔ 客户端中继，含素材管理、NAS 同步与批量调度；整体 Docker 化交付

### Electron 客服工具（client-tool）｜公司项目
- 技术栈：Electron + Vue3 + TypeScript/Pinia
- 多平台客服（WhatsApp/Telegram 等）：会话/群管理、批量消息、自动回复/定时消息、翻译/关键词回复、AI 客服
- SSE + 事件总线联动；Windows 发布；与后端/业务 API 联调

### 818-center｜Electron 中心端（公司项目）
- 技术栈：Electron + Vue3 + TypeScript
- IPC 拉起工具链；主进程：本地 HTTP 静态资源、WebSocket 心跳、自定义协议唤起、多窗口生命周期、优雅退出
- 预加载与代理、自动更新、多环境打包

### Tpush / Tpush-Client｜Twitter/X 运营自动化（公司项目｜广州慧华）
- 形态：Web 管理端 + Electron 客户端，形成「Web 配置 → 客户端执行」链路
- Web 管理端：Vue3 + Vite + TypeScript + Element Plus + Pinia
- Electron 客户端：DelayQueue 统一限流 Browser API；SSE 实时状态联动；Preload/IPC；自动更新；Windows 发布
- 自动化：Puppeteer 执行社媒自动化任务，提升运营效率与执行稳定性

### gost-serve｜Go 代理集群治理（公司项目）
- 技术栈：Go + Redis；HTTP + gRPC
- 账号/流量计费/黑名单/配置治理；Redis 键设计与计费统计
- 按需创建代理与端口分配、配置持久化；统一鉴权/路由与 CLI 子命令、标准响应/日志，减少人工配置与排障成本

### go-zentao-mcp｜禅道 MCP（公司内推广 / 个人主导）
- Go 实现禅道 REST → MCP 工具映射（约 82 个），支持 Cursor/Claude Desktop 等直连
- 提供构建/启动/测试/调试/部署脚本与文档，推动团队内落地使用

### go-code-generator / go-route-converter / go-cmd（Go 提效工具）
- go-code-generator：读取 MySQL 元数据驱动分层 CRUD 生成，实现「配表即生成」
- go-route-converter：AST 解析提取路由信息，生成 JSON/前端配置与可视化维护页面，辅助接口扫描与测试
- go-cmd：零第三方依赖的 Go CLI 库，沉淀子命令/参数解析/帮助模板最佳实践

### ChainTracer｜TRON 地址监控桌面端（个人项目）
- 技术栈：Electron + Vue 3 + TypeScript（electron-vite）
- 监控多个 TRON 地址（支持备注）、汇总展示 USDT（TRC20）转账（筛选/分页/复制地址）、展示各地址 USDT 余额
- 多数据源：Tronscan（默认）/ TronGrid（可选 API Key），配置本地存储（Electron userData）

### automation-ext｜浏览器自动化编排扩展（个人项目）
- 技术栈：Chrome Extension MV3（Service Worker）+ SidePanel + Content Scripts
- 以 **Step 工作流**驱动多 Tab 自动化编排；支持脚本注入、消息通信、流程停止/恢复、进度同步
- 将多页面流程沉淀为可复用编排，强调稳定执行与可维护的工程结构

---

## 工作经历

### 自由职业 / 独立开发（接单）｜2026.04 - 至今
- 以接单形式承接客户端、浏览器扩展与 AI Agent 方向项目，独立完成需求对接、方案设计、开发交付与上线维护
- 已交付：海外社媒 AI 选品系统、Electron 多站点编排客户端、ERP 飞书智能助手（无 API 系统注入接入 + 有 API 系统 OpenAPI 接入双方案）、AI 绘画平台画布自动化、AI 视觉创作平台素材与工作流自动化

### 广州慧华智能科技有限公司｜全栈开发｜2024.12 - 2026.04
- 负责公司 SCRM 及各类端（Web 管理端/后台 + 后端 API + Electron 桌面端）研发与维护
- 负责海外主流社媒相关自动化功能开发（含 Web 管理端 + Electron 客户端）
- 参与竞品能力调研与对接方案沉淀，支撑产品/运营需求

**阶段成果**
- 桌面端「中心端 + 客服工具」架构稳定落地：协议唤起、多窗口生命周期、自动更新等关键链路可用
- 交付 Web 管理端/后台 + 后端 API + 桌面端联动的完整链路，减少跨端联调成本并降低维护成本

### 广东渔人渡食品有限公司｜技术主管｜2021.04 - 2024.07
- 团队管理与交付：制定计划、任务拆解、进度跟踪与资源协调；代码评审/性能优化/技术分享提升团队能力
- 维护与重构：后台管理系统、商家中台、微信小程序持续迭代与体验优化
- 全栈建设与优化：Vue + Go 等技术栈项目持续交付

**阶段成果**
- 保障后台/中台与多套小程序稳定运行并按期迭代，持续提升用户体验与业务支撑效率
- 主导涯涯生活产品线多端研发与重构优化（后台 + 小程序），支撑亲子活动、优惠券、分销、拼团、抽奖等业务闭环
- 落地并推广 go-route-converter、go-code-generator 等内部提效工具，提升接口扫描/统计与 CRUD 生成效率，缩短交付周期

### 广东蓝极熊环保科技有限公司｜技术经理｜2019.04 - 2021.04
- 领导多人技术团队制定计划并推进交付；覆盖 IoT 后台/多套小程序与智能分拣中心配套系统
- 技术栈：Vue + Java（Spring 系）/ 原生小程序 + Java

**阶段成果**
- 北极环：后台管理系统与多套小程序（智能回收/环保卫侠/设备控制器）上线，支撑 IoT 设备管控、C 端回收与现场设备调试
- 蓝极熊：三套小程序与后台管理系统上线，打通「扫码登记 → 质检定价 → 实时打款」闭环，并实现地磅/监控/车牌识别等硬件数据联动
- 在原计划约一年周期内半年完成研发并上线，提前交付支撑试运行与投产节奏

---

## 教育经历
- 广东农工商职业技术学院｜大专｜软件技术｜2016 - 2019
